# media-player-project/flutter-remote/pubspec.yaml
name: flutter_remote
description: A Flutter-based remote control app for the media player.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
